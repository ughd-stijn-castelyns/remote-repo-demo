name: Continuous-Integration-Workflow

on:
    push:
        branches: [main]
        paths:
            - 'src/**'
    pull_request: 
        branches: [main]
        paths:
            - 'src/**'
    workflow_dispatch: 

jobs:
    build:
        runs-on: self-hosted
        steps:
            - run: echo 'Hello World'